<?xml version="1.0"?>
<!--
  @file package.xml
  @brief ROS2 package manifest for the C++ BehaviorTree.CPP runtime.
-->
<package format="3">
  <!-- Package identity shown by colcon and the ament index. -->
  <name>sandwich_bt_runtime_cpp</name>
  <version>0.0.0</version>
  <description>BehaviorTree.CPP runtime for the sandwich task.</description>
  <maintainer email="beniamino.squitieri@iit.it">Beniamino Squitieri</maintainer>
  <license>Apache-2.0</license>

  <!-- Build system used by this ROS2 package. -->
  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- Runtime/build dependencies used by the executable and custom BT nodes. -->
  <depend>ament_index_cpp</depend>
  <depend>rclcpp</depend>
  <depend>sandwich_bt_interfaces</depend>

  <!-- BehaviorTree.CPP dependency used by sandwich_bt_runner. -->
  <depend>behaviortree_cpp</depend>

  <!-- Tell ament this package is built with CMake. -->
  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
