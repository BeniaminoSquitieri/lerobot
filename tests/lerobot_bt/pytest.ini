[pytest]
# Anchors pytest rootdir here so it does not walk up to tests/conftest.py
# which requires the full lerobot ML stack (draccus, torch, etc.).
# Run from ~/lerobot: PYTHONPATH=src python -m pytest tests/lerobot_bt/ -q
